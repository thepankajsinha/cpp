#include <bits/stdc++.h>
using namespace std;

int main()
{

    // take single word as input
    string firstName;
    cin >> firstName;

    //print out the string
    cout << firstName;
    // Pankaj


    // take multiple words as input
    string fullName;
    getline(cin, fullName);

    //print out the string
    cout<<fullName;
    // Pankaj kuamr
}